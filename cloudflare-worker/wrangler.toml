name = "poe2gps-contribute"
main = "worker.js"
compatibility_date = "2026-06-01"

# Rate-limit counter store (5 requests / 60s per CF-Connecting-IP).
# Deliberately NOT using unsafe.bindings native Ratelimit — experimental, defer to v0.22.
# PMS-13 + PMS-15 (v0.23 Signal deploy, 2026-07-10): RATE_KV namespace created and worker.js v3
# deployed. Both /submit-atlas and /submit-trace routes are live on
# poe2gps-contribute.luther-rotmg.workers.dev.
[[kv_namespaces]]
binding = "RATE_KV"
id = "93976850edea44768f286a5b9911b716"
